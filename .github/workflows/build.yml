name: Build Android APK on: [push, workflow_dispatch] jobs: 作业: build: 构建: 构建: runs-on: 运行平台: 运行平台: ubuntu-latest steps: ubuntu-latest 步骤: ubuntu-latest 步骤: - uses: - 使用: - 使用: - 使用: actions/checkout@v4 - name: actions/checkout@v4 - 名称: actions/checkout@v4 - 名称: actions/checkout@v4 - 名称: set up JDK 17 uses: 设置 JDK 17 使用: 设置 JDK 17 使用: 设置 JDK 17 使用: actions/setup-java@v4 with: actions/setup-java@v4 带有: actions/setup-java@v4 带有: java-version: '17' distribution: 'temurin'分发: 'temurin'分发: 'temurin'分发: 'temurin' - name: - 名称: - 名称: Setup Android SDK Environment run: | 设置 Android SDK 环境并运行: | 设置 Android SDK 环境并运行: | 设置 Android SDK 环境并运行: | echo "ANDROID_HOME=$ANDROID_SDK_ROOT"回声"ANDROID_HOME=$ANDROID_SDK_ROOT"回声"ANDROID_HOME=$ANDROID_SDK_ROOT"回声"ANDROID_HOME=$ANDROID_SDK_ROOT" >> $GITHUB_ENV export ANDROID_HOME=$ANDROID_SDK_ROOT # 接受许可并安装指定的 SDK 平台和编译工具 yes | sdkmanager --licenses sdkmanager "platforms;android-34" "build-tools;34.0.0" # 修正 build.sh 里的 ANDROID_HOME 路径 sed -i "s|export ANDROID_HOME=\${ANDROID_HOME:-\$HOME/android-sdk}|export ANDROID_HOME=$ANDROID_SDK_ROOT|g" android/build.sh chmod +x android/build.sh - name: Build APK run: | cd android ./build.sh - name: Upload APK uses: actions/upload-artifact@v4 with: name: Veglia-APK path: android/Veglia.apk
